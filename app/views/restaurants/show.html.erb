<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1><%= @restaurant.name %></h1>
    <%= link_to new_restaurant_review_path(@restaurant.id), class: 'btn btn-light btn-sm border' do %>
      <i class="fa fa-pencil"></i> Review this restaurant
    <% end %>
  </div>


  <p class="well"><%= @restaurant.address %></p>

  <hr>
  <div>
    <p>review content</p>
  </div>
  <%= link_to restaurants_path do %>
    <i class="fa-solid fa-arrow-rotate-left"></i> Back to list
  <% end %>
</div>
